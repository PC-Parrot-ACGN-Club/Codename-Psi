[package]
name = "core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Deterministic rules, match state, config models, input, and replay for Codename Psi"

# Crate name `core` shadows `::core` in rustdoc; unit tests cover this crate.
[lib]
doctest = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
ron = { workspace = true }
thiserror = { workspace = true }
rand = { workspace = true }
rand_chacha = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
